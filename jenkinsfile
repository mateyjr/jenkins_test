pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..1st.master branch'
                // Add build steps like: sh 'make build'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing.1st..master branch'
                // Add test steps like: sh 'make test'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying..1st. master branch'
                // Add deploy steps like: sh 'make deploy'
            }
        }
    }
}
